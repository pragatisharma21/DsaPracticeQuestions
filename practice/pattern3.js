for (let grandfather = 1; grandfather <= 5; grandfather++) {
    let bag = "";
    for (let sam = 1; sam <= 10; sam++) {
        bag += "* ";
    }
    console.log(bag.trim()); // Trim the trailing space
}
